export interface StreamingServiceUrlValues {
    server: string;
    pathStart: string;  // this is the part of the url that comes after the domain and port usually movies or series
    username: string;
    password: string;
  }
  